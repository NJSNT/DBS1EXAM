# DBS Assignment 4

DDL for an association of art galleries

Hand-in info

- Hand in a .pdf file with your answers and SQL statements.
- Additionally, also hand in a .sql file with all your SQL statements (in DataGrip go to File->Save As to save a console as a .sql file).

Introduction:

Your tasks will be to implement the database of a new system for an association of art galleries.

Global Relations Diagram for the art gallery system:

Task A, from global relations to DDL

You are given the Global Relations Diagram above and must implement the database exactly how it appears in the diagram. Various constraints and rules are specified in the comment boxes.

Note: All attributes in the diagram are mandatory unless they are specifically marked as ‘Optional’.

As you are writing the DDL statements, then make sure you:

- Choose data types (include lengths/precision/etc.) according to the comment boxes
- Create all necessary constraints according to the comment boxes
- Create at least a couple of domains, and explain your reasons for your choices
- Add all Primary keys and Foreign keys as shown in the diagram

Task B, creation order

Explain here how you decided in which order to create your tables. Include concrete examples.

Task C, referential actions

Find at least a couple of places where it would make sense to have referential actions (CASCADE, SET NULL, etc.). Explain your reasons here, and then add the referential actions to your DDL statements.

Task D, insert data

Create the necessary SQL statements to insert two rows of valid data into each of the tables you created. Describe any interesting considerations/assumptions/problems/etc. that you might have had or made.

Task E, SQL code

Insert all your SQL statements from the above tasks here (i.e. all DDL and Insert statements).